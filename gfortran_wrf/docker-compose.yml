version: '3.3'

services:
  wrf-runs:
    build:
      context: ../
      dockerfile: gfortran_wrf/Dockerfile
    image: mullenkamp/wrf-auto-runs:2.7
    shm_size: '1gb'
    volumes:
        - "./parameters.toml:/app/parameters.toml"
        - ~/WPS_GEOG:/WPS_GEOG
