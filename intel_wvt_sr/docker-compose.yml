version: '3.3'

services:
  wrf-runs:
    build:
      context: ../
      dockerfile: intel_wvt_sr/Dockerfile
    image: mullenkamp/wrf-auto-runs-intel-wvt-sr:1.0
    shm_size: '1gb'
    volumes:
        - "./parameters.toml:/app/parameters.toml"
        - ~/WPS_GEOG:/WPS_GEOG
