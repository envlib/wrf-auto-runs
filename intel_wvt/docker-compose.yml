version: '3.3'

services:
  wrf-runs:
    build: .
    image: mullenkamp/wrf-auto-runs-intel-wvt:1.2
    shm_size: '1gb'
    volumes:
        - "./parameters.toml:/app/parameters.toml"
        - ~/WPS_GEOG:/WPS_GEOG
